@model IList<VendorModel>
@{
    Layout = "_ColumnsTwo";

    //title
    Html.AddTitleParts(T("PageTitle.Vendors").Text);
    //page class
    Html.AppendPageCssClassParts("html-vendor-list-page");
}
<div class="page vendor-list-page from-panel">
    <div class="page-title panel-heading">
        <h1>@T("Vendors.List")</h1>
    </div>
    <div class="page-body panel-body">
        <div class="vendor-grid">
            <div class="item-grid">
                <div class="row">
                    @foreach (var item in Model)
                    {
                        <div class="item-box col-xs-12 col-sm-4 col-md-4 product-align">
                            <div class="vendor-item thumbnail">
                                <h2 class="title">
                                    <a href="@Url.RouteUrl("Vendor", new { SeName = item.SeName })" title="@item.PictureModel.Title">
                                        @item.Name
                                    </a>
                                </h2>
                                <div class="picture">
                                    <a href="@Url.RouteUrl("Vendor", new { SeName = item.SeName })" title="@item.PictureModel.Title">
                                        <img alt="@item.PictureModel.AlternateText" src="@item.PictureModel.ImageUrl" title="@item.PictureModel.Title" />
                                    </a>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
