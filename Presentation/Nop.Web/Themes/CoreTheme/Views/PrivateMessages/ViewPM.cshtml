@model PrivateMessageModel
@{
    Layout = "_ColumnsOne";

    //title
    Html.AddTitleParts(T("PageTitle.ViewPM").Text);
    //page class
    Html.AppendPageCssClassParts("html-private-messages");
    Html.AppendPageCssClassParts("html-private-message-view-page");
}
<div class="page private-messages private-message-view-page from-panel">
    <div class="page-title panel-heading">
        <h1>@T("PrivateMessages.View.ViewMessage")</h1>
    </div>
    <div class="page-body panel-body">
        <div class="view-message">
            <div>
                <label>@T("PrivateMessages.View.From")</label>
                <span>@Model.CustomerFromName</span>
            </div>
            <div>
                <label>@T("PrivateMessages.View.To")</label>
                <span>@Model.CustomerToName</span>
            </div>
            <div>
                <label>@T("PrivateMessages.View.Subject")</label>
                <span>@Model.Subject</span>
            </div>
            <div class="message">
                <span>@Html.Raw(Model.Message)</span>
            </div>
            <div class="options">
                <input type="button" class="btn btn-default reply-pm-button" value="@T("PrivateMessages.View.ReplyButton")"
                       onclick="setLocation('@Url.RouteUrl("SendPMReply", new { toCustomerId = Model.FromCustomerId, replyToMessageId = Model.Id })')" />
                <input type="button" class="btn btn-default delete-pm-button" value="@T("PrivateMessages.View.DeleteButton")"
                       onclick="setLocation('@Url.RouteUrl("DeletePM", new { privateMessageId = Model.Id })')" />
                <input type="button" class="btn btn-default back-pm-button" value="@T("PrivateMessages.View.BackButton")"
                       onclick="setLocation('@Url.RouteUrl("PrivateMessages")')" />
            </div>
        </div>
    </div>
</div>
